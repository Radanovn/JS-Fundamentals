'use strict';

function lastArray(args) {
   

    let firstNumber = args[0];
    let secondNumber = args[args.length - 1];
    console.log(+firstNumber + +secondNumber);
    
}
lastArray(['20', '30', '40'])